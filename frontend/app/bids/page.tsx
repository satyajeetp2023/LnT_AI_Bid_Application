"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {Card,Badge} from "@/components/ui";import {request} from "@/services/api";import type {Bid} from "@/types";
export default function Bids(){const [bids,setBids]=useState<Bid[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{request<Bid[]>("/bids").then(setBids).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div><div className="flex justify-between"><div><h1 className="text-2xl font-bold text-navy">My Bids</h1><p className="text-sm text-slate-500">Bid projects you are authorized to access</p></div><Link href="/bids/new" className="rounded bg-gold px-4 py-2 text-sm font-bold">+ Create New Bid</Link></div>{loading?<Card className="mt-6 p-10 text-center">Loading bids…</Card>:error?<Card className="mt-6 border-red-200 p-6 text-red-700">Unable to load bids: {error}</Card>:bids.length===0?<Card className="mt-6 p-10 text-center text-slate-500">No bids yet. Create your first bid project to begin.</Card>:<Card className="mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Bid ID","Tender Name","Client","Contract Type","Project Type","Due Date","Stage","Status","Bid Manager",""].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody>{bids.map(b=><tr key={b.id} className="border-t"><td className="px-4 py-3 font-bold text-blue-700">{b.bid_id}</td><td className="px-4 font-semibold">{b.tender_name}</td><td className="px-4">{b.client}</td><td className="px-4">{b.contract_type}</td><td className="px-4">{b.project_type}</td><td className="px-4 whitespace-nowrap">{new Date(b.tender_due_date+"T00:00:00").toLocaleDateString()}</td><td className="px-4"><Badge>{b.current_stage}</Badge></td><td className="px-4"><Badge tone={b.bid_status==="Draft"?"grey":"green"}>{b.bid_status}</Badge></td><td className="px-4 whitespace-nowrap">{b.bid_manager}</td><td className="px-4 whitespace-nowrap"><Link href={`/bids/${b.id}/documents`} className="font-semibold text-blue-700">Open</Link> · <Link href={`/bids/${b.id}/edit`} className="font-semibold text-blue-700">Edit</Link></td></tr>)}</tbody></table></Card>}</div>}
