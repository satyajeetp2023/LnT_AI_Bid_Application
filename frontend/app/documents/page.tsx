import {Card} from "@/components/ui";export default function Documents(){return <div><h1 className="text-2xl font-bold text-navy">Document Repository</h1><p className="mt-1 text-sm text-slate-500">Select a bid from My Bids to view its protected document repository.</p><Card className="mt-6 p-10 text-center text-sm text-slate-500">Documents are partitioned by project and are never exposed through public storage URLs.</Card></div>}
